/* Librerias de Python*/

pip install pytest-xdist  --trusted-host files.pythonhosted.org --trusted-host pypi.org --trusted-host pypi.python.org --default-timeout=100
pip install virtualenv  
pip install selenium  
pip install pytest  
pip install spyder-kernels==2.4.* 
pip install requests  
pip install beautifulsoup4  
pip install lxml  
pip install scrapy  
pip install webdriver-manager  
pip install requests-html  
pip install scrapy-useragents 
pip install jupyterlab 
pip install pymongo 
pip install schedule 
pip install oracledb 
pip install openpyxl 
pip install scrapy-zyte-smartproxy 
pip install cufflinks 
pip install chromedriver_py
pip install chromedriver_py --upgrade

/** Correr un archivo de scrapy en consola*/ 
scrapy runspider 3_Nivel4_W3S.py -o w3s.json -t json

/* Correr un archivo de pytest en consola*/
pytest .\Asserts_3.py -s -v -m login


from selenium.webdriver.chrome.service import Service
from chromedriver_py import binary_path

service=webdriver.ChromeService(executable_path=binary_path)
driver=webdriver.Chrome(service=service)

/** arrancar el server de django**/
pipenv run python manage.py runserver


encoding='ISO-8859-1'

servidor Filezilla
14148
pass: 123654


/*** Chat GPT avanzado**/
beta.openai.com/playground

/** Ejemplos **/ 
https://platform.openai.com/docs/examples


